{
	vars: [x, y, z],
	initnode: node_init,
	nodes: {
	    node_init: {    },
	    node_while: { fancy_prop: [[x > 0]] },
      	node_exit: { },
  },
	transitions: [
		{
			name: t0,
			source: node_init,
			target: node_while,
			constraints: [x' = 50, y' = 0, z'=z]
		},{
			name: t2,
			source: node_while,
			target: node_while,
			constraints: [x >= 1, x' = x - y, y' = y, z'=z]
		},{
			name: t1,
			source: node_while,
			target: node_while,
			constraints: [x >= 10, x' = x - y, y' = y, z'=z]
		}
	]
}
